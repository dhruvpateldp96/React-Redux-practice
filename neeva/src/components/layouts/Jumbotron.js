import React from 'react'

const Jumbotron = ({obj}) => {
    console.log(obj)
    return (
        <div className="jumbotron">
            {/* {obj.forEach((obj) => <h2>{obj}</h2>)} */}
            {/* <h2>{id}</h2>
            <h2>{title}</h2>
            <h2>{date}</h2> */}
        </div>
    )
}

export default Jumbotron
